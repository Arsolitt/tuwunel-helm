version: 2
updates:
  # Keeps the pinned action versions in .github/workflows/ci.yaml current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      github-actions:
        patterns:
          - "*"
