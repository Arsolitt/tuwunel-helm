# MUST be rejected: envFromSecret values must be '<secret-name>/<key>'. Without
# the key part the template fails with an opaque index error.
server_name: matrix.ci.example

envFromSecret:
  LIVEKIT_KEY: livekit-secrets
